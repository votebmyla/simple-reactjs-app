pipeline {
  agent any
  environment {
    DOCKERHUB_CREDENTIALS=credentials('dockerhub')
  }
  stages {
    stage('listing directory'){
      steps{
        sh 'ls -l'
      }
    }
    stage('build docker image'){
      steps{
        echo 'docker build...'
      }
    }
  }
}